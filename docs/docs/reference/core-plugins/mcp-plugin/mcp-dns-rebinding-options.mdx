---
title: "McpDnsRebindingOptions"
generated: true
---
<GenerationInfo sourceFile="packages/mcp-plugin/src/types.ts" sourceLine="205" packageName="@vendure/mcp-plugin" since="3.8.0" />

DNS-rebinding protection for the MCP transport. When `allowedHosts`/`allowedOrigins` are
provided, requests whose `Host`/`Origin` header is not in the list are rejected before the
MCP handler runs. When omitted, the guard is not applied.

```ts title="Signature"
interface McpDnsRebindingOptions {
    allowedHosts?: string[];
    allowedOrigins?: string[];
}
```

<div className="members-wrapper">

### allowedHosts

<MemberInfo kind="property" type={`string[]`}   />


### allowedOrigins

<MemberInfo kind="property" type={`string[]`}   />




</div>
