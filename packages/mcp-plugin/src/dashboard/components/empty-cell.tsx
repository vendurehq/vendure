/**
 * Placeholder for a table cell that has no value, so every empty cell in the
 * MCP tables looks the same.
 */
export function EmptyCell() {
    return <span className="text-muted-foreground">—</span>;
}
